import PagePanel from "../components/PagePanel.jsx";

export default function Prizes() {
  return (
    <PagePanel title="Prizes & Rewards" subtitle="Perks for top contributors">
      <div className="rounded-2xl ring-1 ring-white/10 bg-white/[.03] p-4 text-sm">
        <p>No special prizes available at the moment. Stay tuned!</p>
      </div>
    </PagePanel>
  );
}
